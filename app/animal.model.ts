export class Animal {
  public done: boolean = false;
  constructor(public name: string, public species: string, public age: number, public diet: string, public location: string, public caretaker: number, public sex: string, public likes: string, public dislikes: string, public date: string ) { }
}
